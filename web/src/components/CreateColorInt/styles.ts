import styled from "styled-components";

export const CreateColorIntContainer = styled.div`
  margin-bottom: 24px;
  span {display:inline-block; width:140px; text-align:right}
  button {
    margin-top: 8px;
    border: none;
    border-radius: 4px;

    background: #2a2;
    color: #fff;

    padding: 4px 16px;
  }
`;
